package com.skilldistillery.event.entitytests;

public class PrescriptionTest {
	
	@BeforeEach
	public void setUp() throws Exception {
		emf = Persistence.createEntityManagerFactory("EventJPA");
		em = emf.createEntityManager();
		comment = em.find(Comment.class, 1);
	}

	@AfterEach
	public void tearDown() throws Exception {
		em.close();
		emf.close();
	}
	
	@Test
	public void test_comment_get_data_from_category_table() {
		Comment comment = em.find(Comment.class, 1);
		assertNotNull(comment);
		assertEquals(1, comment.getId());
		assertEquals("Augustus Gibbons", comment.getName());
		assertEquals("This seems dangerous", comment.getContent());
	}

}
